'use strict';

/**
 * translate ascii to peterish
 * @param {string} string ASCII string which should be translated.
 * @returns //TODO Promise
 */
function encode (string) {

    // TODO input validation (is ascii?)

}

/**
 * validates an ASCII string
 * @param {string} string ASCII string to validate.
 * @returns {boolean} boolean Boolean indicating whether string is valid or not.
 */
function validate(string) {

    // TODO is ascii?
}

module.exports = {
    encode,
    validate,
};
